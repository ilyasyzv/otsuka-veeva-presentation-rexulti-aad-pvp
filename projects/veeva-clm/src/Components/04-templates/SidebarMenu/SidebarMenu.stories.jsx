import React from 'react';
import { SidebarMenu } from './SidebarMenu';

export default {
  title: 'CLM/Templates/SidebarMenu',
  component: SidebarMenu,
};

const Template = () => <SidebarMenu />;

export const Default = Template.bind({});
